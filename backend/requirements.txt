# Django 5.2 LTS is the target for deployment and needs Python 3.12.
# The floor is 4.2 LTS so the project also runs on Python 3.9 machines,
# which is what it was verified on locally.
Django>=4.2,<6.0
djangorestframework>=3.15
django-cors-headers>=4.4
dj-database-url>=2.2
python-dotenv>=1.0
psycopg[binary]>=3.2 ; python_version >= "3.10"
psycopg2-binary>=2.9 ; python_version < "3.10"
gunicorn>=22.0
